const get = url => {
    //checks
    return url;
};

const post = url => {
    //checks
    return url;
};

const deleteitem = url => {
    //checks
    return url;
};

const findRightAction = (obj) => {
    //by methon select option. 
}





